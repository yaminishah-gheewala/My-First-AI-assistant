SESSION_SECRET=replace-with-a-long-random-string
# Postgres connection string. On Vercel, adding a Postgres/Neon storage
# integration sets this automatically. For local dev, point it at any
# Postgres database, e.g. postgresql://user:password@localhost:5432/dbname
DATABASE_URL=postgresql://user:password@localhost:5432/health_lab
